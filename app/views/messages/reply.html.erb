<h2>Replying to <%= @user.username %>.</h2>
<h4><%= @user.username %> originally wrote: </h4>
<h4><%= @message.content %><h4>

<%= form_tag("/messages/new", method: "post") do %>
 	 <%= hidden_field_tag(:subject, "re: #{@message.subject}") %>
 	 <%= text_area_tag(:content, nil, placeholder: "content", :cols => "50", :rows => "10") %><br>
 	 <%= hidden_field_tag(:target_id, @user.id) %>
 	 <%= hidden_field_tag(:reciever, @user.username) %>
 	 <%= hidden_field_tag(:sender_id, current_user.id) %>
    <%= submit_tag("submit reply", class: "button") %>
	<% end %>

<style>
#main {
	height: 100vh;
}
</style>